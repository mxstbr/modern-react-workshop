# React Suspense

---

React Suspense is the first feature enabled by concurrent React

---

Rather than rendering all at once and then committing it to the DOM, React can split up the work into chunks and commit them at the end.
